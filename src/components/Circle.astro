
  <!-- Contenedor del badge -->
  <div class="relative w-44 h-44">
    <!-- Círculo / arco -->
    <svg viewBox="0 0 100 100" class="-rotate-90 w-full h-full">
      <!-- arco principal (ajusta dasharray/dashoffset para el tamaño y la “brecha”) -->
      <circle cx="50" cy="50" r="45"
              class="fill-none stroke-amber-50 [stroke-dasharray:260_1000] [stroke-dashoffset:32]"
              stroke-width="1.5"
              stroke-linecap="round" />
      <!-- puntito (colocado sobre la circunferencia) -->
      <circle cx="50" cy="5" r="2.3" class="fill-amber-50" />
    </svg>

    <!-- Texto centrado -->
    <div class="absolute inset-0 grid place-items-center text-center">
      <div>
        <div class="text-5xl font-bold text-sky-200 leading-none">20</div>
        <div class="text-sky-200/90 font-semibold -mt-1">
          Años de<br/>Experiencia
        </div>
      </div>
    </div>
  </div>